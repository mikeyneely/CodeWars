// We need a function that can transform a number into a string.
//
// What ways of achieving this do you know?
// String() function will convert the value of an object to a string.

function numberToString(num) {
return String(num)
}
